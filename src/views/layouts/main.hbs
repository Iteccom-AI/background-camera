<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}}</title>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Poppins", sans-serif;
        border-radius: 10px;
      }

      body {
        width: 100vw;
        height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      main {
        flex: 1;
      }
      footer {
        width: 100%;
        height: 50px;
        display: flex;
        justify-content: center;
        align-items: center;
        shrink: 0;
      }

      input {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
      }
    </style>
  </head>

  <body>
    <main>
      {{{body}}}  {{!-- Đây là nơi view con (ví dụ home.hbs) được render vào --}}
    </main>

    <footer>
      <p style="text-align:center; color:#aaa;">
        &copy; {{year}} Background Camera Service
      </p>
    </footer>
  </body>
</html>
